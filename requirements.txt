# Core test framework
pytest==8.3.3
pytest-playwright==0.5.0
pip install python-dotenv

# Playwright browser automation
playwright==1.48.0

# Allure reporting integration
allure-pytest==2.13.5

# Optional but recommended helpers
pytest-base-url==2.1.0    # for managing base URLs easily
pytest-html==4.1.1        # optional HTML reports (in addition to Allure)
requests==2.32.3          # useful if you make API calls in tests

# Code quality (optional)
flake8==7.1.1
black==24.8.0
